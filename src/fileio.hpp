#ifndef FILEIO_HPP
#define FILEIO_HPP

#include <iostream>
#include <fstream>
#include <string>

std::string read_shader(std::string filename);
std::string read_kernel_function(std::string filename);


#endif
